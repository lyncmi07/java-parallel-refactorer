/**
 * The ParallelExecutor class that is used by the programmer to 
 * call on parallel code they have created using the library uses the ThreadAllocator to 
 * process individual tasks to be run in parallel.
 */
package parallel.threadallocation;